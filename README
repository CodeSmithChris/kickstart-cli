To use the setup script make sure to run from inside the 
main configs repo

pwd should return ~/.../configs
run sudo ./setup.sh

After the script completes feel free to delete this repo if desired


